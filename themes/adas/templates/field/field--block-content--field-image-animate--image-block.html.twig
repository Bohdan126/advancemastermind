{% for item in items %}
	<img class="img-responsive animate-in" src="{{ item.content['#item'].entity.uri.value|image_create_url }}" data-anim-delay="100" data-anim-type="fade-in-left" alt="{{ item.content['#item'].alt }}">
{% endfor%}
